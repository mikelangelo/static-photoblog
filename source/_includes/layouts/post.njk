---
layout: layouts/base.njk
permalink: /posts/{{ title|slugURL }}-{{ date|slugURL }}/
---

<div class="container">
	{# <h1>{{ title }}</h1> #}
	<div class="pb-5 border-bottom mb-5">
		{{ content | safe }}
	</div><!-- end spacing -->

		{# {%- set previousPost = collections.posts | getPreviousCollectionItem(page) %}
		{%- if previousPost %}
			<div> <a href="{{ previousPost.url | url }}"><< {{ previousPost.data.title }}</a></div>
		{% endif %}

		{%- set nextPost = collections.posts | getNextCollectionItem(page) %}
		{%- if nextPost %}
			<div> <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }} >></a> </div>
		{% endif %} #}
		{% include 'source/pages/page-components/post-previous-next.njk' %}

</div><!-- end container -->


